nodeType: example-node-type
component: ExampleNode
file: webview-src/nodes/ExampleNode.tsx
category: control-flow | integration | transform | ui | debug
label: Example
purpose: One sentence describing what this node does.
handles:
  inputs:
    - id: input
      position: top
      semantic: in
      rotatesWithConnection: true
  outputs:
    - id: output
      position: bottom
      semantic: next
      rotatesWithConnection: true
    - id: error
      position: right
      semantic: error
runtime:
  canStartFlow: false
  debugStartSupported: false
capabilities:
  - id: example-capability
    kind: generate | suggest | transform | validate | explain | debug
    title: Human Readable Capability Name
    prompt: Short instruction for what the AI should do for this node.
    inputs:
      - currentFlow
      - selectedNode
    outputs:
      - result
