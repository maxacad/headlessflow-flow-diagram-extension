flow: "Error"
kind: "view"

entries:
  - id: "Start1"
    name: "Start"
    mode: "private"
    strict: false
    inputs:
      []
  - id: "StartNode1"
    name: "FileNotFound"
    mode: "private"
    strict: false
    inputs:
      []

steps:
  - id: "InteractionEnd2"
    type: "interaction"
  - id: "InteractionEnd3"
    type: "interaction"

links:
  - from: "Start1"
    via: "next"
    to: "InteractionEnd2"
    into: "in"
  - from: "StartNode1"
    via: "next"
    to: "InteractionEnd3"
    into: "in"
